image: archlinux

packages:
  - pkg-config
  - cmake
  - make
  - freetype2
  - fontconfig
  - libxcb
  - libxkbcommon
  - scdoc

sources:
  - https://github.com/acreetionos-terminal/acreetionos-terminal

environment:
  PATH: /home/build/.cargo/bin:/usr/bin/

tasks:
  - rustup: |
      curl https://sh.rustup.rs -sSf | sh -s -- -y --default-toolchain stable --profile minimal
  - rustfmt: |
      cd acreetionos-terminal
      rustup toolchain install nightly -c rustfmt
      cargo +nightly fmt -- --check
  - man-pages: |
      cd acreetionos-terminal
      cat extra/man/acreetionos-terminal.1.scd | scdoc > /dev/null
      cat extra/man/acreetionos-terminal-msg.1.scd | scdoc > /dev/null
      cat extra/man/acreetionos-terminal.5.scd | scdoc > /dev/null
      cat extra/man/acreetionos-terminal-bindings.5.scd | scdoc > /dev/null
  - test: |
      cd acreetionos-terminal
      cargo test
  - oldstable: |
      cd acreetionos-terminal
      oldstable=$(cat Cargo.toml | grep "rust-version" | sed 's/.*"\(.*\)".*/\1/')
      rustup toolchain install --profile minimal $oldstable
      rustup default $oldstable
      cargo test
  - clippy: |
      cd acreetionos-terminal
      rustup component add clippy
      cargo clippy --all-targets
  - feature-wayland: |
      cd acreetionos-terminal/acreetionos-terminal
      RUSTFLAGS="-D warnings" cargo test --no-default-features --features=wayland
  - feature-x11: |
      cd acreetionos-terminal/acreetionos-terminal
      RUSTFLAGS="-D warnings" cargo test --no-default-features --features=x11
